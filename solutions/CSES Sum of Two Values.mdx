---
id: cses-1640
title: CSES Sum of Two Values
author: Michael Cao
---

Given an array of $n$ elements, you are asked to find two values which sum to $x$.

Let's start by iterating over the first value in $\mathcal{O}(n)$ time. Given one value, $a$, the other value must be $x - a$ unless $a > x$ (in which case $a$ cannot be a valid first value).

So the question reduces to, given some value $a$, does some other value $x - a$ exist? 

# Using a Set

One idea that comes to mind would be to used a boolean array to store the values. Unfortunately, since $a_i \leq 10^9$, this approach isn't feasible.

However, we can store the values in an (un)ordered set, and use the `.contains` method to check whether a value exists. 

The last detail we need to deal with is that the problem asks us to return the indices of the values, rather than the values themselves. To deal with this, let's find the two correct values first, and then find two values which have these indices. 

To be careful not to count the same index twice, we'll add values to the set as we iterate through it, so at some index $i$ you only consider values with index $j < i$.

<LanguageSection>

<CPPSection>

```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vi = vector<int>;
#define pb push_back
#define rsz resize
#define all(x) begin(x), end(x)
#define sz(x) (int)(x).size()
using pi = pair<int,int>;
#define f first
#define s second
#define mp make_pair
void setIO(string name = "") { // name is nonempty for USACO file I/O
    ios_base::sync_with_stdio(0); cin.tie(0); // see Fast Input & Output
    if(sz(name)){
        freopen((name+".in").c_str(), "r", stdin); // see Input & Output
        freopen((name+".out").c_str(), "w", stdout);
    }
}

int main() {
    int n, x; cin >> n >> x;
    vi a(n);
    set<int> vals;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    int v1 = -1, v2 = -1;
    for (int i = 0; i < n; i++) {
        if(vals.count(x - a[i])){
            v1 = i + 1;
            for(int j = 0; j < i; j++){
                if(a[j] == (x - a[i])){
                    v2 = j + 1;
                    cout << v1 << " " << v2 << "\n";
                    return 0;
                }
            }
        }
        vals.insert(a[i]);
    }
    cout << "IMPOSSIBLE" << '\n';
}
```

</CPPSection>

</LanguageSection>

<IncompleteSection>
</IncompleteSection>