---
id: usaco-994
source: USACO Gold 2020 January
title: Farmer John Solves 3SUM
author: Qi Wang
---

[Official Analysis](http://www.usaco.org/current/data/sol_threesum_gold_jan20.html)

## Implementation

**Time Complexity:** $\mathcal{O}(N^2)$

<LanguageSection>

<JavaSection>

```java
import java.util.*;
import java.io.*;

public class Threesum {
	public static void main(String[] args) throws IOException {
		Kattio io = new Kattio("threesum");

		int N = io.nextInt();
		int Q = io.nextInt();
		int[] a = new int[N];
		long[][] dp = new long[N+1][N+1];
		int[] cnt = new int[2 * 1000000 + 1];

		for (int i = 0; i < N; i++) {
			a[i] = io.nextInt();
		}

		for (int i = 0; i < N - 1; i++) {
			for (int j = 0; j < N; j++) {
				cnt[a[j] + 1000000] = 0;
			}

			for (int j = i+1; j < N; j++) {
				int k = -a[i] - a[j];
				if(k >= -1000000 && k <= 1000000) {
					dp[i+1][j+1] += cnt[k + 1000000];
				}

				cnt[a[j] + 1000000]++;
			}
		}
		
		for (int i = N; i >= 1; i--) {
			for (int j = i+1; j <= N; j++) {
				dp[i][j] += dp[i+1][j] + dp[i][j-1] - dp[i+1][j-1];
			}
		}

		for (int i = 0; i < Q; i++) {
			int a1 = io.nextInt();
			int b = io.nextInt();

			io.println(dp[a1][b]);
		}

		io.close();
	}
	private static class Kattio extends PrintWriter {
		private BufferedReader r;
		private StringTokenizer st;

		// standard input
		public Kattio() { this(System.in,System.out); }
		public Kattio(InputStream i, OutputStream o) {
			super(o);
			r = new BufferedReader(new InputStreamReader(i));
		}
		// USACO-style file input
		public Kattio(String problemName) throws IOException {
			super(new FileWriter(problemName+".out"));
			r = new BufferedReader(new FileReader(problemName+".in"));
		}

		// returns null if no more input
		public String next() {
			try {
				while (st == null || !st.hasMoreTokens())
					st = new StringTokenizer(r.readLine());
				return st.nextToken();
			} catch (Exception e) {}
			return null;
		}

		public int nextInt() { return Integer.parseInt(next()); }
		public double nextDouble() { return Double.parseDouble(next()); }
		public long nextLong() { return Long.parseLong(next()); }
	}
}

```

</JavaSection>

</LanguageSection>
