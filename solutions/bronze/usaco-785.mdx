---
id: usaco-785
source: USACO Bronze 2018 January
title: Out of Place
author: Maggie Liu, Ryan Chou, Vikas Thoutam
---

[Official Analysis (Java)](http://www.usaco.org/current/data/sol_outofplace_bronze_jan18.html)

## Video Solution

By Vikas Thoutam

<Youtube id="FaoAL7Prc6o" />

<Spoiler title="Video Solution Code">
<LanguageSection>
<CPPSection>

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
	// file input and output
	freopen("outofplace.in", "r", stdin);
	freopen("outofplace.out", "w", stdout);

	// Read in the array size (n) and create the original and sorted arrays,
	// as well as the difference variable
	int n;
	cin >> n;
	int og[n];
	int sorted[n];
	int diff = 0;

	// Take in the values of the array as input,
	// and place them in both original and sorted
	for (int i = 0; i < n; i++)
	{
		cin >> og[i];
		sorted[i] = og[i];
	}

	// Actually sort the "sorted" array
	sort(sorted, sorted + n);

	// Count the number of differences in the arrays
	for (int i = 0; i < n; i++)
	{
		if (og[i] != sorted[i])
		{
			diff++;
		}
	}

	// Print 0 if there are no differences (no swaps) or
	// diff - 1 if there are differences
	cout << max(0, diff - 1);

	return 0;
}
```

</CPPSection>
<JavaSection>

```java
import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.io.PrintWriter;
import java.util.Arrays;
public class OutofPlace {
	public static void main(String[] args) throws IOException {
		// File input and output
		BufferedReader br = new BufferedReader(new FileReader("outofplace.in"));
		PrintWriter pw = new PrintWriter("outofplace.out");
		
		// Read in the array size (n) and create the original and sorted arrays, 
		// as well as the difference variable
		int n = Integer.parseInt(br.readLine());
		int[] og = new int[n];
		int[] sort = new int[n];
		int diff = 0;
		
		// Take in the values of the array as input, 
		// and place them in both original and sorted in the same order
		for (int i = 0; i < n; i++) 
		{
			og[i] = Integer.parseInt(br.readLine());
			sort[i] = og[i];
		}
		
		// Actually sort the "sorted" array
		Arrays.sort(sort);
		
		// Compute the number of positions that are 
		// different between the og and sorted arrays
		for ( int i = 0; i < n; i++) 
		{
			if (og[i] != sort[i]) 
			{
				diff++;
			}
		}
		
		// Print 0 if there are no differences (no swaps) or diff - 1. If there
		// are differences (you need one swap for each difference, minus 1)
		pw.println(Math.max(0, diff - 1));
		pw.close();
		
	}
}
```

</JavaSection>
</LanguageSection>
</Spoiler>

## Implementation

**Time Complexity:** $\mathcal{O}(N\log{N})$

<LanguageSection>
<PySection>

```py
fin = open('outofplace.in', 'r')
N = int(fin.readline())

order = [int(fin.readline()) for _ in range(N)]
ordsort = sorted(order)

swaps = 0
for i in range(N):
	if order[i] != ordsort[i]:
		swaps += 1

print(swaps - 1, file=open('outofplace.out', 'w'))
```

</PySection>
<CPPSection>

```cpp
#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;

int main()
{
	freopen("outofplace.in", "r", stdin);
	freopen("outofplace.out", "w", stdout);
	int n;
	cin >> n;
	int cows[100], sorted[100];
	for (int i = 0; i < n; i++)
	{
		cin >> cows[i];
		sorted[i] = cows[i];
	}
	sort(sorted, sorted + n);
	int swaps = 0;
	for (int i = 0; i < n; i++)
	{
		if (cows[i] != sorted[i])
		{
			swaps++;
		}
	}
	cout << swaps - 1 << endl;
	return 0;
}
```

</CPPSection>
</LanguageSection>
