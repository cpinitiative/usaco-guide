---
id: usaco-1587
source: USACO Bronze 2026 Third Contest
title: Make All Distinct
author: Junwon Kim
---

[Official Analysis (C++)](https://usaco.org/current/data/sol_prob1_bronze_season26contest3.html)

## Explanation

Two elements can only collide if they share the same remainder modulo $|K|$, so we handle each remainder class independently.

Within each class, sort the elements in the direction of $K$ (ascending if $K > 0$, descending if $K < 0$). Then iterate in sorted order, advancing each element only as much as strictly needed to make it distinct from the previous one. This greedy approach is optimal because we never move an element more than necessary.

## Implementation

**Time Complexity:** $\mathcal{O}(N \log N)$

<LanguageSection>
<CPPSection>

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int t;
	cin >> t;
	while (t--) {
		int n, k;
		cin >> n >> k;

		int absk = abs(k);
		map<int, vector<int>> groups;
		for (int i = 0; i < n; i++) {
			int a;
			cin >> a;
			groups[((a % absk) + absk) % absk].push_back(a);
		}

		long long ans = 0;
		for (auto &[r, v] : groups) {
			if (k > 0) {
				sort(v.begin(), v.end());
			} else {
				sort(v.begin(), v.end(), greater<int>());
			}
			int prev = v[0];
			for (int i = 1; i < (int)v.size(); i++) {
				if (k > 0) {
					int next = max(v[i], prev + absk);
					ans += (next - v[i]) / absk;
					prev = next;
				} else {
					int next = min(v[i], prev - absk);
					ans += (v[i] - next) / absk;
					prev = next;
				}
			}
		}

		cout << ans << "\n";
	}
	return 0;
}
```

</CPPSection>
</LanguageSection>
