export default function useProblemSuggestionAction() {
  return async ({
    name,
    link,
    difficulty,
    tags,
    additionalNotes,
    problemTableLink,
  }) => {
    // @jeffrey todo
  };
}
